interface GuardianType {
  id: number; // Adjust based on your API response
  name: string; // Adjust based on your API response
  status: 'Active' | 'Inactive'; // Status can only be 'Active' or 'Inactive'
}
