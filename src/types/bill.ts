interface Bill {
    cash?: number ; // Optional cash property
    check?: number; // Optional check property
    date?: string; // Optional date property
    names?: string; // Optional names property
  }
  