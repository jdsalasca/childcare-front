interface Bill {
    cash?: string | number; // Optional cash property - can be string or number
    check?: string | number; // Optional check property - can be string or number
    date?: string; // Optional date property
    names?: string; // Optional names property
  }
  