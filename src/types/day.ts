interface DayType {
    id: string; // Adjust based on your API response
    name: string; // Adjust based on your API response
    laboral_day: boolean; // Adjust based on your API response
    label?: string; // Optional property for label
    value?: string; // Optional property for value,
    translationLabel: string; // Optional property for translation label
  }
  